en:
  classes:
    cmor/core/backend/engine: Core
  cmor:
    core:
      backend:
        resources_controller:
          base:
            show_actions:
              back: Back
              destroy: Destroy
              edit: Edit
              history: History
        settings_sidebars:
          show:
            font_size: "Fontsize"
  routes:
    cmor-core-backend-engine: core
  or: or
